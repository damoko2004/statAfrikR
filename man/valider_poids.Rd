% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sondage.R
\name{valider_poids}
\alias{valider_poids}
\title{Valider les poids de sondage}
\usage{
valider_poids(saf_design, population_cible = NULL, seuil_outlier = 3)
}
\arguments{
\item{saf_design}{saf_design -- Objet cree par \code{creer_design()}}

\item{population_cible}{numeric ou NULL -- Population totale cible
(pour verifier que sum(poids) est coherent). Defaut : NULL}

\item{seuil_outlier}{numeric -- Seuil de detection des outliers en
ecarts-types (score Z). Defaut : 3.0}
}
\value{
Un tibble avec les statistiques de validation par strate
}
\description{
Verifie la coherence des poids de sondage : somme par strate,
detection des poids aberrants (outliers), DEFF eleves, comparaison
avec la population cible. Etape indispensable avant toute analyse.
}
\examples{
set.seed(42)
n <- 300
donnees <- data.frame(
  poids_sond = runif(n, 1800, 3500),
  strate     = sample(c("Urbain","Rural"), n, TRUE),
  grappe     = sample(1:20, n, TRUE)
)
design <- suppressMessages(
  creer_design(donnees,"poids_sond",var_strate="strate",var_grappe="grappe"))
valider_poids(design)

}
