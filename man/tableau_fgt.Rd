% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pauvrete.R
\name{tableau_fgt}
\alias{tableau_fgt}
\title{Tableau institutionnel des indices FGT}
\usage{
tableau_fgt(
  fgt_obj,
  format = c("tibble", "flextable", "excel"),
  chemin = NULL,
  titre = NULL,
  inclure_sous_groupes = TRUE
)
}
\arguments{
\item{fgt_obj}{objet \code{saf_fgt} -- Resultat de \code{calcul_fgt()}}

\item{format}{character -- Format de sortie : \code{"tibble"},
\code{"flextable"} ou \code{"excel"}. Defaut : "tibble"}

\item{chemin}{character ou NULL -- Chemin du fichier Excel.
Si NULL, utilise \code{tempdir()}. Defaut : NULL}

\item{titre}{character -- Titre du tableau}

\item{inclure_sous_groupes}{logical -- Inclure les tableaux par
sous-groupe. Defaut : TRUE}
}
\value{
Tibble, flextable ou chemin du fichier Excel
}
\description{
Genere un tableau des indices FGT formate selon les
conventions des INS africains et de la Banque mondiale (EHCVM).
}
\examples{
set.seed(42)
menages <- data.frame(
  depense_pc = c(rexp(70, rate = 1/150000), rexp(30, rate = 1/400000)),
  poids      = runif(100, 0.8, 1.2)
)
fgt <- calcul_fgt(menages, "depense_pc", 220000, poids = "poids")
tableau_fgt(fgt)

}
