% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/traitement.R
\name{imputer_valeurs}
\alias{imputer_valeurs}
\title{Imputer les valeurs manquantes}
\usage{
imputer_valeurs(
  data,
  vars = NULL,
  methode = c("mediane", "moyenne", "mode", "hot_deck", "regression"),
  vars_auxiliaires = NULL,
  graine = 42L,
  rapport = TRUE
)
}
\arguments{
\item{data}{data.frame ou tibble — Données avec valeurs manquantes}

\item{vars}{character ou NULL — Variables à imputer. Si NULL, toutes les
variables avec valeurs manquantes. Défaut : NULL.}

\item{methode}{character — Méthode d'imputation :
\code{"mediane"}, \code{"moyenne"}, \code{"mode"},
\code{"hot_deck"}, \code{"regression"}. Défaut : "mediane".}

\item{vars_auxiliaires}{character ou NULL — Variables auxiliaires pour
l'imputation par régression ou hot-deck. Défaut : NULL.}

\item{graine}{integer — Graine aléatoire pour la reproductibilité.
Défaut : 42.}

\item{rapport}{logical — Retourner un rapport d'imputation. Défaut : TRUE.}
}
\value{
Si \code{rapport = FALSE} : tibble imputé.
Si \code{rapport = TRUE} : liste avec \code{$donnees} et \code{$rapport}.
}
\description{
Impute les valeurs manquantes d'un dataset selon la méthode
spécifiée. Supporte l'imputation simple (statistiques descriptives),
hot-deck et par régression. Produit un rapport de traçabilité.
}
\examples{
\dontrun{
  resultat <- imputer_valeurs(
    data    = donnees_enquete,
    vars    = c("revenu_mensuel", "age"),
    methode = "mediane"
  )
  donnees_propres <- resultat$donnees
}
}
