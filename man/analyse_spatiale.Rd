% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyse.R
\name{analyse_spatiale}
\alias{analyse_spatiale}
\title{Analyse spatiale — jointure et indicateurs par zone}
\usage{
analyse_spatiale(
  data,
  shapefile,
  var_geo_data,
  var_geo_shape,
  indicateurs = NULL,
  fonctions = list(moyenne = function(x) mean(x, na.rm = TRUE), n = function(x)
    sum(!is.na(x)))
)
}
\arguments{
\item{data}{data.frame ou tibble — Données avec variable géographique}

\item{shapefile}{sf ou character — Objet sf ou chemin vers un fichier
shapefile (.shp, .gpkg, .geojson)}

\item{var_geo_data}{character — Variable géographique dans \code{data}}

\item{var_geo_shape}{character — Variable géographique dans le shapefile}

\item{indicateurs}{character ou NULL — Variables à agréger par zone.
Si NULL, toutes les variables numériques. Défaut : NULL.}

\item{fonctions}{list — Fonctions d'agrégation nommées.
Défaut : \code{list(moyenne = mean, n = length)}.}
}
\value{
Un objet \code{sf} avec les indicateurs calculés par zone.
}
\description{
Joint un jeu de données statistiques avec un shapefile
géographique et calcule des indicateurs par aire géographique.
Produit un objet sf enrichi prêt pour la cartographie.
}
\examples{
\dontrun{
  carte <- analyse_spatiale(
    data          = donnees_enquete,
    shapefile     = "data/shapefiles/regions.shp",
    var_geo_data  = "region",
    var_geo_shape = "NOM_REGION",
    indicateurs   = c("taux_pauvrete", "revenu_moyen")
  )
}
}
