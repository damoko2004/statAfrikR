% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ipm.R
\name{tableau_ipm}
\alias{tableau_ipm}
\title{Tableau institutionnel IPM}
\usage{
tableau_ipm(
  res_ipm,
  pays = "Pays",
  annee = as.integer(format(Sys.Date(), "\%Y")),
  chemin_word = NULL
)
}
\arguments{
\item{res_ipm}{saf_ipm -- Resultat de calcul_ipm() ou
calcul_ipm_national()}

\item{pays}{character -- Nom du pays. Defaut : "Pays"}

\item{annee}{integer -- Annee de reference}

\item{chemin_word}{character ou NULL -- Chemin Word (.docx).
Si NULL, retourne l'objet flextable. Defaut : NULL}
}
\value{
Un objet \code{flextable} (invisible si chemin_word fourni)
}
\description{
Genere un tableau flextable institutionnel (style INS/PARIS21)
des resultats IPM, exportable en Word ou Excel.
}
\examples{
set.seed(42)
n <- 200
menages <- data.frame(
  nutrition   = rbinom(n,1,0.35), electricite = rbinom(n,1,0.60),
  eau         = rbinom(n,1,0.40), poids = runif(n,0.8,1.3)
)
res <- calcul_ipm(menages, var_nutrition="nutrition",
  var_electricite="electricite", var_eau="eau", poids="poids")
tableau_ipm(res, pays="Centrafrique", annee=2026L)

}
