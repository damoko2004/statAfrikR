% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sante.R
\name{retard_croissance}
\alias{retard_croissance}
\title{Calculer le taux de retard de croissance (stunting)}
\usage{
retard_croissance(
  donnees,
  var_taille_age_z = NULL,
  var_taille = NULL,
  var_age_mois = NULL,
  var_sexe = NULL,
  poids = NULL,
  sous_groupes = NULL,
  seuil = -2
)
}
\arguments{
\item{donnees}{data.frame -- Donnees enfants < 5 ans}

\item{var_taille_age_z}{character -- Score Z taille-pour-age
(HAZ). Si NULL, calcule depuis var_taille, var_age et var_sexe.
Defaut : NULL}

\item{var_taille}{character ou NULL -- Taille en cm. Defaut : NULL}

\item{var_age_mois}{character ou NULL -- Age en mois. Defaut : NULL}

\item{var_sexe}{character ou NULL -- Sexe (1=garcon, 2=fille ou
"M"/"F"). Defaut : NULL}

\item{poids}{character ou NULL -- Variable de ponderation.
Defaut : NULL}

\item{sous_groupes}{character ou NULL -- Variables de desagregation.
Defaut : NULL}

\item{seuil}{numeric -- Seuil (score Z). Defaut : -2}
}
\value{
Un objet de classe \code{saf_anthropo}
}
\description{
Calcule la prevalence du retard de croissance chez les
enfants de moins de 5 ans (taille-pour-age < -2 ecarts-types selon
les standards OMS 2006). ODD 2.2.1.
}
\examples{
set.seed(42)
n <- 500
enfants <- data.frame(
  haz   = rnorm(n, -1.2, 1.3),
  milieu = sample(c("urbain","rural"), n, TRUE),
  poids  = runif(n, 0.7, 1.4)
)
retard_croissance(enfants, var_taille_age_z = "haz", poids = "poids")

}
