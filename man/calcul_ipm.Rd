% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ipm.R
\name{calcul_ipm}
\alias{calcul_ipm}
\title{Calculer l'Indice de Pauvrete Multidimensionnelle (IPM)}
\usage{
calcul_ipm(
  donnees,
  var_nutrition = NULL,
  var_mortalite_inf = NULL,
  var_annees_scol = NULL,
  var_scolarisation = NULL,
  var_combustible = NULL,
  var_assainissement = NULL,
  var_eau = NULL,
  var_electricite = NULL,
  var_logement = NULL,
  var_actifs = NULL,
  poids = NULL,
  seuil_k = 1/3,
  ic = FALSE,
  n_bootstrap = 200L
)
}
\arguments{
\item{donnees}{data.frame -- Donnees menages}

\item{var_nutrition}{character ou NULL -- Variable nutrition
(1 = prive, 0 = non prive). Defaut : NULL}

\item{var_mortalite_inf}{character ou NULL -- Deces d'enfant
dans le menage (1 = oui). Defaut : NULL}

\item{var_annees_scol}{character ou NULL -- Aucun membre >= 6 ans
ayant complete 6 ans de scolarite (1 = prive). Defaut : NULL}

\item{var_scolarisation}{character ou NULL -- Enfant non scolarise
dans le menage (1 = prive). Defaut : NULL}

\item{var_combustible}{character ou NULL -- Combustible solide
pour la cuisine (1 = prive). Defaut : NULL}

\item{var_assainissement}{character ou NULL -- Assainissement
non ameliore (1 = prive). Defaut : NULL}

\item{var_eau}{character ou NULL -- Eau non potable (1 = prive).
Defaut : NULL}

\item{var_electricite}{character ou NULL -- Sans electricite
(1 = prive). Defaut : NULL}

\item{var_logement}{character ou NULL -- Logement inadequat
(1 = prive). Defaut : NULL}

\item{var_actifs}{character ou NULL -- Pas d'actifs de base
(1 = prive). Defaut : NULL}

\item{poids}{character ou NULL -- Variable de ponderation.
Defaut : NULL (poids egaux)}

\item{seuil_k}{numeric -- Seuil de pauvrete multidimensionnelle
(proportion de privations). Defaut : 1/3}

\item{ic}{logical -- Calculer les intervalles de confiance
(bootstrap). Defaut : FALSE}

\item{n_bootstrap}{integer -- Nombre de replications bootstrap.
Defaut : 200L}
}
\value{
Un objet de classe \code{saf_ipm} contenant :
\code{IPM}, \code{H}, \code{A}, \code{contributions},
\code{n_obs}, \code{seuil_k}, \code{methode}
}
\description{
Calcule l'IPM standard PNUD/OPHI selon la methode
Alkire-Foster (2011). L'IPM = H x A, ou H est l'incidence
(proportion de menages multidimensionnellement pauvres) et A
l'intensite moyenne de privation parmi les pauvres.
}
\examples{
set.seed(42)
n <- 200
menages <- data.frame(
  nutrition      = rbinom(n, 1, 0.35),
  mortalite_inf  = rbinom(n, 1, 0.12),
  annees_scol    = rbinom(n, 1, 0.40),
  scolarisation  = rbinom(n, 1, 0.28),
  combustible    = rbinom(n, 1, 0.55),
  assainissement = rbinom(n, 1, 0.48),
  eau            = rbinom(n, 1, 0.38),
  electricite    = rbinom(n, 1, 0.62),
  logement       = rbinom(n, 1, 0.42),
  actifs         = rbinom(n, 1, 0.30),
  poids          = runif(n, 0.8, 1.3)
)
res <- calcul_ipm(menages,
  var_nutrition      = "nutrition",
  var_mortalite_inf  = "mortalite_inf",
  var_annees_scol    = "annees_scol",
  var_scolarisation  = "scolarisation",
  var_combustible    = "combustible",
  var_assainissement = "assainissement",
  var_eau            = "eau",
  var_electricite    = "electricite",
  var_logement       = "logement",
  var_actifs         = "actifs",
  poids              = "poids"
)
print(res)

}
\references{
Alkire, S. & Foster, J. (2011). Counting and
multidimensional poverty measurement.
\emph{Journal of Public Economics}, 95(7-8), 476-487.
\doi{10.1016/j.jpubeco.2010.11.006}
}
