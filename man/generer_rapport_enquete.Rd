% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rapports.R
\name{generer_rapport_enquete}
\alias{generer_rapport_enquete}
\title{Generer un rapport d'enquete menage parametre}
\usage{
generer_rapport_enquete(
  donnees,
  meta = list(),
  template = c("enquete_menage", "bulletin"),
  sortie = c("html", "pdf", "word"),
  chemin_sortie = tempdir(),
  vars_analyse = NULL,
  var_poids = NULL,
  ouvrir = FALSE
)
}
\arguments{
\item{donnees}{data.frame -- Donnees d'enquete menage}

\item{meta}{list -- Metadonnees du rapport. Elements attendus :
\code{pays}, \code{titre}, \code{annee}, \code{source},
\code{auteur} (tous optionnels)}

\item{template}{character -- Nom du template :
\code{"enquete_menage"} ou \code{"bulletin"}.
Defaut : "enquete_menage"}

\item{sortie}{character -- Format de sortie : \code{"html"},
\code{"pdf"} ou \code{"word"}. Defaut : "html"}

\item{chemin_sortie}{character -- Repertoire de destination.
Defaut : \code{tempdir()}}

\item{vars_analyse}{character ou NULL -- Variables a analyser.
Si NULL, detecte automatiquement les variables numeriques.
Defaut : NULL}

\item{var_poids}{character ou NULL -- Variable de ponderation.
Defaut : NULL}

\item{ouvrir}{logical -- Ouvrir le rapport apres generation.
Defaut : FALSE}
}
\value{
Chemin du fichier genere (invisible)
}
\description{
Genere un rapport statistique complet a partir de donnees
d'enquete menage, en utilisant un template R Markdown pre-construit.
Le rapport inclut une page de garde, des statistiques descriptives,
une pyramide demographique et les indicateurs cles.
}
\examples{
\dontrun{
  set.seed(42)
  donnees <- data.frame(
    age    = sample(18:70, 200, replace = TRUE),
    revenu = rexp(200, rate = 1/250000),
    sexe   = sample(c("H","F"), 200, TRUE),
    milieu = sample(c("urbain","rural"), 200, TRUE),
    poids  = runif(200, 0.8, 1.3)
  )
  meta <- list(
    pays   = "Republique Centrafricaine",
    titre  = "Enquete sur les conditions de vie des menages",
    annee  = 2026L,
    source = "ICASEES",
    auteur = "Direction des Statistiques"
  )
  generer_rapport_enquete(donnees, meta, sortie = "html")
}

}
