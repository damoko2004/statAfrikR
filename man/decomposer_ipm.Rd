% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ipm.R
\name{decomposer_ipm}
\alias{decomposer_ipm}
\title{Decomposer l'IPM par sous-groupe}
\usage{
decomposer_ipm(res_ipm, donnees, var_groupe, poids = NULL)
}
\arguments{
\item{res_ipm}{saf_ipm -- Resultat de \code{calcul_ipm()} ou
\code{calcul_ipm_national()}}

\item{donnees}{data.frame -- Donnees originales (meme ordre que
lors du calcul de l'IPM)}

\item{var_groupe}{character -- Variable de sous-groupe
(ex : "region", "milieu", "sexe_cm")}

\item{poids}{character ou NULL -- Variable de ponderation.
Defaut : NULL}
}
\value{
Un tibble avec H, A, IPM, n_obs et contribution par groupe
}
\description{
Decompose l'IPM par region, milieu ou sexe du chef de
menage. Retourne H, A et IPM par groupe, ainsi que la contribution
de chaque groupe a l'IPM national.
}
\examples{
set.seed(42)
n <- 300
menages <- data.frame(
  nutrition      = rbinom(n, 1, 0.35),
  electricite    = rbinom(n, 1, 0.60),
  eau            = rbinom(n, 1, 0.40),
  milieu         = sample(c("urbain","rural"), n, TRUE),
  poids          = runif(n, 0.8, 1.3)
)
res <- calcul_ipm(menages,
  var_nutrition = "nutrition", var_electricite = "electricite",
  var_eau = "eau", poids = "poids")
decomposer_ipm(res, menages, var_groupe = "milieu", poids = "poids")

}
