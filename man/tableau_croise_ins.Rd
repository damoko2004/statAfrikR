% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tableaux.R
\name{tableau_croise_ins}
\alias{tableau_croise_ins}
\title{Tableau croise pondere - format institutionnel INS}
\usage{
tableau_croise_ins(
  data,
  ligne,
  colonne,
  poids = NULL,
  type_pct = c("colonne", "ligne", "total"),
  marges = TRUE,
  chi2 = TRUE,
  format = c("tibble", "flextable", "excel"),
  chemin = NULL,
  titre = NULL
)
}
\arguments{
\item{data}{data.frame, tibble ou \code{svydesign} -- Donnees}

\item{ligne}{character -- Variable en ligne}

\item{colonne}{character -- Variable en colonne}

\item{poids}{character ou NULL -- Variable de ponderation. Defaut : NULL}

\item{type_pct}{character -- \code{"colonne"}, \code{"ligne"} ou
\code{"total"}. Defaut : "colonne"}

\item{marges}{logical -- Afficher les totaux. Defaut : TRUE}

\item{chi2}{logical -- Calculer le test du chi-deux. Defaut : TRUE}

\item{format}{character -- \code{"tibble"}, \code{"flextable"} ou
\code{"excel"}. Defaut : "tibble"}

\item{chemin}{character ou NULL -- Chemin Excel. Defaut : NULL}

\item{titre}{character ou NULL -- Titre. Defaut : NULL}
}
\value{
Tibble, flextable ou chemin Excel
}
\description{
Produit un tableau de contingence pondere avec marges,
pourcentages et test du chi-deux.
}
\examples{
set.seed(42)
donnees <- data.frame(
  region = sample(c("Nord", "Sud", "Est", "Ouest"), 300, TRUE),
  milieu = sample(c("urbain", "rural"), 300, TRUE, prob = c(0.4, 0.6)),
  poids  = runif(300, 0.7, 1.4)
)
tableau_croise_ins(donnees, "region", "milieu", poids = "poids")

}
