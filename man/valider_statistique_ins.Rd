% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation_ins.R
\name{valider_statistique_ins}
\alias{valider_statistique_ins}
\title{Valider la qualite statistique d'une enquete (INS Validation Suite)}
\usage{
valider_statistique_ins(
  donnees,
  var_poids = NULL,
  var_region = NULL,
  var_milieu = NULL,
  var_depense = NULL,
  seuil_pauvrete = NULL,
  dictionnaire = NULL,
  seuil_na_alerte = 0.05,
  seuil_cv_alerte = 33,
  seuil_cellule = 5L,
  pays = "Pays",
  annee = as.integer(format(Sys.Date(), "\%Y"))
)
}
\arguments{
\item{donnees}{data.frame -- Donnees de l'enquete}

\item{var_poids}{character ou NULL -- Variable de ponderation. Defaut : NULL}

\item{var_region}{character ou NULL -- Variable region/prefecture.
Defaut : NULL}

\item{var_milieu}{character ou NULL -- Variable milieu. Defaut : NULL}

\item{var_depense}{character ou NULL -- Variable de consommation/depense
pour le calcul FGT. Defaut : NULL}

\item{seuil_pauvrete}{numeric ou NULL -- Seuil de pauvrete. Defaut : NULL}

\item{dictionnaire}{data.frame ou NULL -- Dictionnaire des variables avec
colonnes : variable, label, type_attendu. Defaut : NULL}

\item{seuil_na_alerte}{numeric -- Seuil de \% valeurs manquantes
declenchant une alerte. Defaut : 0.05 (5\%)}

\item{seuil_cv_alerte}{numeric -- Seuil de CV declenchant une alerte.
Defaut : 33.0}

\item{seuil_cellule}{integer -- Seuil de confidentialite. Defaut : 5L}

\item{pays}{character -- Nom du pays. Defaut : "Pays"}

\item{annee}{integer -- Annee de l'enquete. Defaut : annee courante}
}
\value{
Un objet de classe \code{saf_validation_ins} avec le rapport
complet et le statut global
}
\description{
Produit un rapport de validation complet couvrant 10 criteres
de qualite statistique institutionnelle : donnees, poids, plan de
sondage, valeurs manquantes, geographie, precision, confidentialite,
metadonnees, reproductibilite et indicateurs cles.
Conforme aux standards IHSN / PARIS21 / ONU.
}
\examples{
set.seed(42)
n <- 500
donnees <- data.frame(
  region  = sample(c("Nord","Sud","Est","Ouest"), n, TRUE),
  milieu  = sample(c("urbain","rural"), n, TRUE),
  poids   = runif(n, 800, 3500),
  depense = pmax(10000, rnorm(n, 165000, 90000)),
  sexe    = sample(c("H","F"), n, TRUE),
  age     = sample(15:80, n, TRUE),
  stringsAsFactors = FALSE
)
valider_statistique_ins(donnees,
  var_poids  = "poids",
  var_region = "region",
  var_milieu = "milieu",
  pays       = "Centrafrique",
  annee      = 2024L)

}
