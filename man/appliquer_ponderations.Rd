% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/traitement.R
\name{appliquer_ponderations}
\alias{appliquer_ponderations}
\title{Appliquer les pondérations d'enquête}
\usage{
appliquer_ponderations(
  data,
  var_poids,
  var_strate = NULL,
  var_grappe = NULL,
  var_fpc = NULL,
  normaliser = FALSE
)
}
\arguments{
\item{data}{data.frame ou tibble — Données de l'enquête}

\item{var_poids}{character — Nom de la variable de pondération finale}

\item{var_strate}{character ou NULL — Variable de stratification.
Défaut : NULL.}

\item{var_grappe}{character ou NULL — Variable d'unité primaire de
sondage (UPS/cluster). Défaut : NULL.}

\item{var_fpc}{character ou NULL — Variable de correction pour
population finie (FPC). Défaut : NULL.}

\item{normaliser}{logical — Normaliser les poids pour que leur somme
soit égale à l'effectif de l'échantillon. Défaut : FALSE.}
}
\value{
Un objet \code{svydesign} du package \code{survey}.
}
\description{
Crée un objet de plan de sondage complexe à partir d'un
tibble et des variables de pondération, strates et grappes. Enveloppe
ergonomique autour de \code{survey::svydesign()} avec validation
complète des poids et messages d'erreur en français.
}
\examples{
\dontrun{
  plan <- appliquer_ponderations(
    data       = donnees_menages,
    var_poids  = "poids_final",
    var_strate = "strate",
    var_grappe = "grappe_id"
  )
}
}
\seealso{
\code{\link{tab_croisee}}, \code{\link{stat_descr}}
}
