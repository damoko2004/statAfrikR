% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genre.R
\name{handicap_prevalence}
\alias{handicap_prevalence}
\title{Calculer la prevalence du handicap}
\usage{
handicap_prevalence(donnees, vars_handicap, poids = NULL, sous_groupes = NULL)
}
\arguments{
\item{donnees}{data.frame -- Donnees individuelles}

\item{vars_handicap}{named character -- Vecteur nomme : type de
handicap -> variable 0/1. Ex: c(Visuel="hand_vis", Moteur="hand_mot")}

\item{poids}{character ou NULL -- Variable de ponderation. Defaut : NULL}

\item{sous_groupes}{character ou NULL -- Variables de desagregation.
Defaut : NULL}
}
\value{
Un tibble avec prevalence par type de handicap
}
\description{
Prevalence du handicap par type (visuel, auditif, moteur,
cognitif) selon la methodologie Washington Group / recensement.
}
\examples{
set.seed(42)
n <- 500
individus <- data.frame(
  hand_vis = rbinom(n, 1, 0.04),
  hand_aud = rbinom(n, 1, 0.03),
  hand_mot = rbinom(n, 1, 0.05),
  sexe     = sample(c("H","F"), n, TRUE),
  poids    = runif(n, 0.8, 1.3)
)
handicap_prevalence(individus,
  vars_handicap = c(Visuel="hand_vis", Auditif="hand_aud",
                    Moteur="hand_mot"),
  poids = "poids")

}
