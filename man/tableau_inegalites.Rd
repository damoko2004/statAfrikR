% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inegalites.R
\name{tableau_inegalites}
\alias{tableau_inegalites}
\title{Tableau de bord des inegalites}
\usage{
tableau_inegalites(
  donnees,
  var_revenu,
  poids = NULL,
  var_groupe = NULL,
  pays = "Pays",
  annee = as.integer(format(Sys.Date(), "\%Y"))
)
}
\arguments{
\item{donnees}{data.frame -- Donnees menages}

\item{var_revenu}{character -- Variable de revenu}

\item{poids}{character ou NULL -- Variable de ponderation. Defaut : NULL}

\item{var_groupe}{character ou NULL -- Variable de groupe pour
decomposition. Defaut : NULL}

\item{pays}{character -- Nom du pays. Defaut : "Pays"}

\item{annee}{integer -- Annee. Defaut : annee courante}
}
\value{
Un tibble avec toutes les mesures d'inegalite
}
\description{
Calcule toutes les mesures d'inegalite en une seule fonction
et les presente dans un tableau institutionnel. Inclut Gini, Palma,
Theil, Atkinson et les parts par quintile.
}
\examples{
set.seed(42)
menages <- data.frame(
  depense = c(rexp(200, 1/150000), rexp(100, 1/500000)),
  milieu  = sample(c("urbain","rural"), 300, TRUE),
  poids   = runif(300, 0.8, 1.3)
)
tableau_inegalites(menages, "depense", poids="poids",
                   var_groupe="milieu", pays="Centrafrique",
                   annee=2026L)

}
