% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pauvrete.R
\name{calcul_fgt}
\alias{calcul_fgt}
\title{Calcul des indices de pauvrete FGT}
\usage{
calcul_fgt(
  data,
  var_depense,
  seuil_pauvrete,
  poids = NULL,
  strate = NULL,
  grappe = NULL,
  sous_groupes = NULL,
  alpha = c(0, 1, 2),
  ic = TRUE,
  na.rm = TRUE
)
}
\arguments{
\item{data}{data.frame, tibble ou objet \code{svydesign} -- Donnees source}

\item{var_depense}{character -- Nom de la variable de depenses ou revenus
par tete (en monnaie locale, strictement positive)}

\item{seuil_pauvrete}{numeric -- Seuil de pauvrete en monnaie locale.
Meme unite que \code{var_depense}}

\item{poids}{character ou NULL -- Nom de la variable de ponderation.
Ignore si \code{data} est un \code{svydesign}. Defaut : NULL}

\item{strate}{character ou NULL -- Variable de stratification.
Defaut : NULL}

\item{grappe}{character ou NULL -- Variable d'identifiant de grappe.
Defaut : NULL}

\item{sous_groupes}{character ou NULL -- Variables de decomposition
(region, milieu, sexe). Defaut : NULL}

\item{alpha}{numeric -- Parametre de sensibilite : 0, 1, 2 ou vecteur.
Defaut : \code{c(0, 1, 2)}}

\item{ic}{logical -- Calculer les intervalles de confiance a 95\%.
Defaut : TRUE}

\item{na.rm}{logical -- Exclure les valeurs manquantes. Defaut : TRUE}
}
\value{
Un objet de classe \code{saf_fgt}
}
\description{
Calcule les indices Foster-Greer-Thorbecke (FGT0, FGT1, FGT2)
avec prise en compte optionnelle du plan de sondage complexe. Les
trois indices mesurent respectivement l'incidence, la profondeur et la
severite de la pauvrete monetaire.
}
\examples{
set.seed(42)
menages <- data.frame(
  depense_pc = c(rexp(70, rate = 1/150000), rexp(30, rate = 1/400000)),
  poids      = runif(100, 0.8, 1.2),
  region     = sample(c("Bangui", "Ombella", "Lobaye"), 100, TRUE),
  milieu     = sample(c("urbain", "rural"), 100, TRUE, prob = c(0.4, 0.6))
)
fgt <- calcul_fgt(menages, var_depense = "depense_pc",
                  seuil_pauvrete = 220000, poids = "poids")
print(fgt)

}
\references{
Foster, J., Greer, J., & Thorbecke, E. (1984). A class of decomposable
poverty measures. Econometrica, 52(3), 761-766.
\doi{10.2307/1913475}
}
