% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diffusion.R
\name{anonymiser_donnees}
\alias{anonymiser_donnees}
\title{Anonymiser un jeu de données}
\usage{
anonymiser_donnees(
  data,
  vars_supprimer = NULL,
  vars_masquer = NULL,
  vars_perturber = NULL,
  vars_generaliser = NULL,
  niveau_bruit = 0.05,
  graine = 42L,
  rapport = TRUE
)
}
\arguments{
\item{data}{data.frame ou tibble — Données à anonymiser}

\item{vars_supprimer}{character ou NULL — Variables à supprimer
entièrement. Défaut : NULL.}

\item{vars_masquer}{character ou NULL — Variables à remplacer par
des codes anonymes. Défaut : NULL.}

\item{vars_perturber}{character ou NULL — Variables numériques à
perturber par bruit aléatoire. Défaut : NULL.}

\item{vars_generaliser}{list ou NULL — Liste nommée de variables à
généraliser avec les bornes : \code{list(age = 5, revenu = 10000)}.
Défaut : NULL.}

\item{niveau_bruit}{numeric — Niveau de bruit pour la perturbation
(proportion de l'écart-type). Défaut : 0.05.}

\item{graine}{integer — Graine aléatoire. Défaut : 42.}

\item{rapport}{logical — Produire un rapport d'anonymisation. Défaut : TRUE.}
}
\value{
Si \code{rapport = FALSE} : tibble anonymisé.
Si \code{rapport = TRUE} : liste avec \code{$donnees} et \code{$rapport}.
}
\description{
Applique les techniques d'anonymisation conformes aux
standards IHSN/PARIS21 : suppression, masquage, generalisation,
perturbation et pseudonymisation des variables sensibles.
}
\examples{
\dontrun{
  resultat <- anonymiser_donnees(
    donnees_enquete,
    vars_supprimer  = c("nom", "prenom", "telephone"),
    vars_masquer    = c("id_menage", "id_individu"),
    vars_perturber  = c("revenu_mensuel"),
    vars_generaliser = list(age = 5)
  )
  donnees_anon <- resultat$donnees
}
}
